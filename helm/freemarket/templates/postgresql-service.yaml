{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "freemarket.postgresql.fullname" . }}
  labels:
    {{- include "freemarket.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: postgresql
      port: 5432
      targetPort: postgresql
      protocol: TCP
  selector:
    {{- include "freemarket.postgresql.selectorLabels" . | nindent 4 }}
{{- end }}
