apiVersion: v1
kind: Service
metadata:
  name: {{ include "freemarket.fullname" . }}
  labels:
    {{- include "freemarket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "freemarket.selectorLabels" . | nindent 4 }}
